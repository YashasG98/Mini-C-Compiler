#include <stdio.h>
#include <stdlib.h>

int max(int a, float b) {
    int a = 10;
    if (x) {
        float b = (a+x);
        int y;
    }
}

int main() {
    int c;
    do {
        int d;
    } while (!n);
}